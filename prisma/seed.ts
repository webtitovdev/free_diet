import { PrismaClient } from "@prisma/client";

const prisma = new PrismaClient();

/**
 * Database Seed Script
 *
 * Заполняет базу данных начальными данными для разработки и тестирования
 *
 * Использование:
 * npx prisma db seed
 */
async function main() {
  console.log("🌱 Начинаем seed базы данных...");

  // Очистка существующих данных (опционально - раскомментируйте если нужно)
  // console.log('🗑️  Очистка существующих данных...')
  // await prisma.dailyLog.deleteMany()
  // await prisma.foodItem.deleteMany()
  // await prisma.meal.deleteMany()
  // await prisma.foodPhoto.deleteMany()
  // await prisma.userProfile.deleteMany()
  // await prisma.user.deleteMany()

  // Примеры популярных продуктов (опционально - для будущего расширения)
  // В данный момент приложение использует OpenRouter API для распознавания,
  // но seed может быть полезен для тестирования или локальной разработки

  console.log("✅ Seed выполнен успешно!");
  console.log("");
  console.log("📝 Примечание: В текущей версии seed не создает тестовые данные.");
  console.log("   Приложение использует:");
  console.log("   - NextAuth для управления пользователями");
  console.log("   - OpenRouter API для распознавания продуктов");
  console.log("   - Реальные данные создаются через UI");
  console.log("");
  console.log("💡 Для тестирования:");
  console.log("   1. Зарегистрируйтесь через UI (/register)");
  console.log("   2. Загрузите фото еды (/photo)");
  console.log("   3. Сохраните приемы пищи");
  console.log("   4. Проверьте календарь прогресса (/calendar)");
}

main()
  .then(async () => {
    await prisma.$disconnect();
  })
  .catch(async (e) => {
    console.error("❌ Ошибка при выполнении seed:", e);
    await prisma.$disconnect();
    process.exit(1);
  });
