# Free Diet - Трекер Калорий по Фотографии

Веб-приложение для отслеживания калорий через анализ фотографий еды. Загрузите фото блюда, и система автоматически определит продукты, вес и калорийность.

## Основные возможности

- 📸 **Анализ по фото**: Автоматическое распознавание продуктов на фотографии с помощью AI
- ✏️ **Редактирование порций**: Уточнение веса продуктов с автоматическим пересчетом калорий
- 👤 **Персональные цели**: Рекомендации по калорийности на основе параметров и целей
- 📅 **Календарь прогресса**: Визуализация достижения целей по дням
- 🔐 **Безопасная аутентификация**: Google OAuth и Email/Password регистрация
- 📊 **Детальная статистика**: БЖУ (белки, жиры, углеводы) для каждого продукта

## Технологический стек

- **Frontend**: Next.js 14+ (App Router), React 18, TypeScript 5 (strict mode)
- **UI**: Ant Design 5.x, Feature-Sliced Design архитектура
- **State Management**: Zustand 4.x
- **Backend**: Next.js API Routes
- **Database**: PostgreSQL + Prisma 5.x ORM
- **Authentication**: NextAuth.js v5 (Auth.js)
- **File Storage**: Vercel Blob (30-day auto-deletion)
- **Email**: Resend + React Email templates
- **AI**: OpenRouter API (Claude Haiku/Sonnet) для распознавания продуктов
- **Nutritional Data**: USDA FoodData Central API (fallback)

## Быстрый старт

### Предварительные требования

- Node.js 18+ и npm
- PostgreSQL база данных
- Учетные записи для внешних сервисов:
  - Google OAuth (Client ID & Secret)
  - OpenRouter API (API Key)
  - Vercel Blob (Read/Write Token)
  - Resend (API Key)

### Установка

1. **Клонируйте репозиторий**

```bash
git clone <repository-url>
cd free_diet
```

2. **Установите зависимости**

```bash
npm install
```

3. **Настройте переменные окружения**

Скопируйте `.env.example` в `.env` и заполните все значения:

```bash
cp .env.example .env
```

Обязательные переменные:

```env
DATABASE_URL=postgresql://user:password@localhost:5432/free_diet
NEXTAUTH_SECRET=your-secret-key-here
NEXTAUTH_URL=http://localhost:3000
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret
OPENROUTER_API_KEY=your-openrouter-api-key
BLOB_READ_WRITE_TOKEN=your-vercel-blob-token
RESEND_API_KEY=your-resend-api-key
```

4. **Проверьте переменные окружения**

```bash
node scripts/check-env.js
```

5. **Настройте базу данных**

```bash
# Создать и применить миграции
npm run db:migrate

# Сгенерировать Prisma Client
npm run db:generate

# Опционально: заполнить базу тестовыми данными
npm run db:seed
```

6. **Запустите приложение в режиме разработки**

```bash
npm run dev
```

Откройте [http://localhost:3000](http://localhost:3000) в браузере.

## Доступные команды

### Разработка

```bash
npm run dev          # Запустить dev сервер
npm run build        # Собрать production build
npm run start        # Запустить production сервер
npm run lint         # Проверить код с ESLint
npm run lint:fix     # Исправить ошибки ESLint автоматически
npm run format       # Форматировать код с Prettier
npm run type-check   # Проверить типы TypeScript
```

### База данных

```bash
npm run db:generate  # Сгенерировать Prisma Client
npm run db:migrate   # Создать и применить миграцию
npm run db:studio    # Открыть Prisma Studio (GUI для БД)
npm run db:seed      # Заполнить БД начальными данными
npm run db:reset     # Сбросить БД и применить все миграции
```

## Структура проекта

```text
src/
├── app/              # Next.js App Router (роутинг, layouts)
│   ├── (auth)/       # Группа auth routes (login, register)
│   ├── (dashboard)/  # Защищенные routes (profile, calendar, photo)
│   └── api/          # Next.js API routes (backend)
├── pages/            # Страницы (композиция виджетов)
├── widgets/          # Виджеты (крупные UI блоки)
├── features/         # Фичи (действия пользователя)
├── entities/         # Бизнес-сущности
└── shared/           # Переиспользуемые ресурсы
    ├── ui/           # UI-kit компоненты
    ├── api/          # API клиенты
    ├── lib/          # Утилиты
    └── config/       # Конфигурация
```

Проект использует **Feature-Sliced Design (FSD)** архитектуру для масштабируемости и поддерживаемости кода.

## Конфигурация внешних сервисов

### Google OAuth

1. Перейдите в [Google Cloud Console](https://console.cloud.google.com/)
2. Создайте новый проект или выберите существующий
3. Включите Google+ API
4. Создайте OAuth 2.0 credentials
5. Добавьте authorized redirect URI: `http://localhost:3000/api/auth/callback/google`

### OpenRouter API

1. Зарегистрируйтесь на [OpenRouter](https://openrouter.ai/)
2. Создайте API key
3. Добавьте ключ в `.env` как `OPENROUTER_API_KEY`

### Vercel Blob Storage

1. Создайте проект на [Vercel](https://vercel.com/)
2. Перейдите в Storage → Create Database → Blob
3. Скопируйте `BLOB_READ_WRITE_TOKEN` в `.env`

### Resend Email

1. Зарегистрируйтесь на [Resend](https://resend.com/)
2. Создайте API key
3. Добавьте ключ в `.env` как `RESEND_API_KEY`

## Разработка

### Архитектурные принципы

Проект следует строгим архитектурным принципам (см. `CLAUDE.md`):

1. ✅ TypeScript strict mode без `any`
2. ✅ Feature-Sliced Design (FSD) архитектура
3. ✅ Prettier + ESLint + Husky pre-commit hooks
4. ✅ Ant Design как единственная UI библиотека
5. ✅ Zustand для управления состоянием
6. ✅ Axios для HTTP requests
7. ✅ Ручное тестирование как primary QA метод

### Добавление новой функции

1. Создайте спецификацию в `specs/`
2. Запланируйте архитектуру с помощью `/speckit.plan`
3. Сгенерируйте задачи с помощью `/speckit.tasks`
4. Реализуйте задачи последовательно
5. Протестируйте вручную все user stories
6. Создайте PR и запросите review

## User Stories

### 1. Регистрация и Вход (P1) 🎯

- Регистрация через Google OAuth или Email/Password
- Подтверждение email для email регистрации
- Вход в систему с сохранением сессии

### 2. Анализ Фотографии Еды (P2)

- Загрузка фото (до 10 MB)
- Автоматическое распознавание продуктов
- Отображение веса, калорий и БЖУ
- Ручное добавление пропущенных продуктов

### 3. Редактирование и Сохранение Приема Пищи (P3)

- Редактирование веса продуктов
- Автоматический пересчет калорий
- Выбор категории приема пищи (завтрак/обед/ужин/перекус)
- Сохранение в профиль

### 4. Управление Профилем и Целями (P4)

- Ввод базовых параметров (вес, рост, возраст, пол)
- Выбор цели (набор/удержание/похудение/контроль сахара)
- Автоматический расчет рекомендуемой калорийности

### 5. Календарь Прогресса (P5)

- Просмотр календаря с индикаторами достижения целей
- Зеленые дни = цель достигнута (±10%)
- Детали дня: все приемы пищи и отклонение от цели

## Деплой

### Vercel (рекомендуется)

1. Подключите репозиторий к Vercel
2. Настройте environment variables в Vercel dashboard
3. Подключите PostgreSQL базу (Vercel Postgres или внешнюю)
4. Deploy автоматически происходит при push в main branch

### Self-hosted

```bash
# Соберите production build
npm run build

# Запустите production сервер
npm start
```

Убедитесь, что все environment variables настроены на production сервере.

## Troubleshooting

### База данных не подключается

- Проверьте `DATABASE_URL` в `.env`
- Убедитесь, что PostgreSQL запущен
- Проверьте, что порт не занят

### OpenRouter API ошибки

- Проверьте корректность `OPENROUTER_API_KEY`
- Убедитесь, что на балансе есть средства
- Проверьте rate limits API

### Ошибки при загрузке фото

- Убедитесь, что `BLOB_READ_WRITE_TOKEN` настроен
- Проверьте размер файла (максимум 10 MB)
- Проверьте формат файла (JPG, PNG, HEIC)

## Лицензия

MIT

## Поддержка

Для вопросов и предложений создайте issue в репозитории.

---

**Разработано с использованием Claude Code** 🤖
